import styled from 'styled-components';
import ComponentAddon from '../ComponentAddon/ComponentAddon';

const InputAddon = styled(ComponentAddon).attrs({
  marginSize: 2,
})`
  width: 24px;
  height: 24px;
`;

export default InputAddon;
