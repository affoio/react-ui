import styled from 'styled-components';
import colors from '../../colors';

const SelectNoOptions = styled.div`
  width: 100%;
  color: ${colors.black[400]};
  text-align: center;
`;

export default SelectNoOptions;
